// Re-export the consolidated Sanity client from sanity/lib/client
// This keeps existing imports like `import {sanity} from '@/lib/sanity'` working
export { client as sanity } from '@/sanity/lib/client'
